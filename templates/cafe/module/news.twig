<div class="news-container">
    {% if news|length > 0 %}
        {% for singleNews in news %}
            {% if singleNews.hasEvent is same as (true) %}
                {% include 'module/partials/eventNewsPartial.twig' %}
            {% else %}
                {% include 'module/partials/newsPartial.twig' %}
            {% endif %}
        {% endfor %}
    {% else %}
        <div class="no-news page-container">
            Es sind keine News vorhanden. Bitte schauen Sie später erneut nach.
        </div>
    {% endif %}
</div>
<div class="reservierung-container page-container">
    <div class="reservierung-banderole banderole"><h1>Reservierung</h1></div>

    {% if sentReservierung is defined %}
        {% if sentReservierung == true %}
            <div class="notification success">
                Vielen Dank für Ihre Reservierung
            </div>
        {% endif %}

        {% if sentReservierung == false %}
            <div class="notification error">
                Es ist etwas beim Versand der Reservierung schief gegangen. Wir entschuldigen uns für die
                Unanehmlichkeiten. Bitte versuchen Sie es erneut. Sollte das Problem weiterhin bestehen, bitte
                kontaktieren Sie uns unter der 01525-5366682
            </div>
        {% endif %}
    {% endif %}
    <div class="reservierung-form">
        <form action="{{ route('reservierung-add') }}" method="POST" name="reservierung-form">
            <div class="flex">
                <div>
                    <label for="name">
                        Ihr Name<br>
                        <input type="text" name="name" required/>
                    </label>
                </div>
                <div>
                    <label for="seats">
                        Wie viele Personen?<br>
                        <input type="number" name="seats" min="1" max="20" required/>
                    </label>
                </div>
                <div>
                    <label for="datum">
                        Wann wollen Sie kommen?<br>
                        <input type="datetime-local" name="datum" required/>
                    </label>
                </div>
            </div>

            <div>
                <div>
                    <label for="email">
                        Emailadresse <br>
                        <input type="email" name="email">
                    </label>
                </div>

                <div class="extra-container">
                    <label for="extras">
                        Sonderwünsche? (Couchplatz gewünscht, ...)<br>
                        <textarea name="extras" cols="30" rows="10"></textarea>
                    </label>
                </div>
            </div>
            <div>
                <input type="submit" name="submit" value="absenden">
            </div>
        </form>
    </div>
</div>